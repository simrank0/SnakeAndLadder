package die;

/**
 * Interface defining the functionality for a die roller.
 */
public interface DiceRoller {
    /**
     * Rolls the dice and returns the result as an integer.
     *
     * @return An integer representing the dice roll result.
     */
    int roll();
}