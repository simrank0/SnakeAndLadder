package game;

/**
 * An enum representing the different game states in Snake and Ladders.
 */
public enum GameState {
    /**
     * Indicates that a player is currently rolling the dice.
     */
    ROLLING_DICE,
    /**
     * Indicates that the game has ended.
     */
    GAME_OVER
}